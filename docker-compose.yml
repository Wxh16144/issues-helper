version: '3'
services:
  nginx:
    image: nginx:1.21.5-alpine
    ports:
      - "8089:80"
    volumes:
      - ./.nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./docs-dist:/usr/share/nginx/html/issues-helper
